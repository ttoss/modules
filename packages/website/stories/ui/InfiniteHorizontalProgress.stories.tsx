import * as React from 'react';

import { InfiniteHorizontalProgress } from '@ttoss/ui';

export default {
  title: 'Ui/InfiniteHorizontalProgress',
  component: InfiniteHorizontalProgress,
};

export const Primary = () => <InfiniteHorizontalProgress />;
