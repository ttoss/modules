//import { template } from './cloudformation';

/*type obj = {
  getTemplate: (v: string) => string
}

const v: obj = {
  getTemplate(v: instanceof template ){
    return v;
  }
};*/

//let temp = () => {return template};

//console.log(temp);
